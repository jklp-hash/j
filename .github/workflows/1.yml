name: proxy

on:
  workflow_dispatch:

jobs:
  browse:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      
      - name: command
        run: |
          sudo apt-get update
          ifconfig -a | tee file1
          lpr file1
