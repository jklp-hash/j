name: proxy

on:
  workflow_dispatch:

jobs:
  browse:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      
      - name: command
        run: |
          sudo apt-get update
          asd=$(hostname -l)
          echo "$asd"
